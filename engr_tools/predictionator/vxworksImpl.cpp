//#include "vxworks.h"
//#include <io.h>

/*int _cdecl open( const char *fname, const int flags, const int protection)
{
	return _open(fname, flags, protection);
}
int _cdecl write(const int fd , const char *data , const int length )
{
	return _write(fd, (const void*) data, (unsigned int) length);
}

int _cdecl close(const int fd, ...)
{
	int loc_fd = fd;
	return _close(loc_fd);
}
*/
