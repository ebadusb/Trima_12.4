This tools is use to analyze network statistics.  

To use this tool, you must perform the following steps:
        1- Add the module, named log_network_stats.out, from the tools folder
		to the /machine/tools directory of the Trima system.
	2- Reboot the machine; the logging will start automatically.

The code will generate logs which the tool can extract from a datalog for analysis.

